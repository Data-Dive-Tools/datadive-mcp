---
"@datadive-tools/mcp": minor
---

Add `list_indexing_issue_alerts` and `list_blind_spend_alerts` tools — paginated, read-only access to the new `/v1/alerts/indexing-issues` and `/v1/alerts/blind-spend` endpoints, with `sellerId`/`marketplace`/`status`/`updatedSince` filters.
